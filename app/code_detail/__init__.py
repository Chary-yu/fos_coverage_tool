"""Canonical Code Detail services and sidecar support.

The package initializer stays import-light because the legacy parser imports
the sidecar package while it is being used as a compatibility dependency.
"""
