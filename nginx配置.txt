server {
    listen 80;
    server_name _;

    access_log /var/log/nginx/coverage_access.log;
    error_log  /var/log/nginx/coverage_error.log;

    # TODO: 改成你们真实办公网段。多个网段写多行 allow。
    allow 10.190.167.0/24;
    deny all;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "same-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self';" always;

    location ~ /\.source_cache {
        deny all;
        return 404;
    }

    location /coverage/ {
        alias /opt/coverage_reports/review/;
        index html/index.html index.html;
        try_files $uri $uri/ =404;
    }

    location /api/coverage {
        proxy_pass http://127.0.0.1:9528/api/coverage;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        client_max_body_size 2m;
    }
}