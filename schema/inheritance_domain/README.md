# Inheritance Domain schema

Inheritance tables are defined by the VNext core and domain DDL under `scripts/upgrade/`. The canonical owners are `app.inheritance.engine`, `app.inheritance.rejections`, `app.services.inheritance_review_service` and `app.db.repositories.analysis_domain_repository`.
