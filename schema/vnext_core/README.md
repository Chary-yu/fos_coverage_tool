# VNext core schema

Authoritative DDL: [`scripts/upgrade/vnext_schema.sql`](../../scripts/upgrade/vnext_schema.sql). This directory is the stable schema ownership label used by release audits; do not edit a second SQL copy here. Validate it with `scripts/upgrade/schema_preflight.py` and the Gate A evidence builder.
